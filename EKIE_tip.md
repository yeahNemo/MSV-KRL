### InstanceOf 三元组损失函数的详细解析

#### 一、InstanceOf 三元组的定义与核心目标

**任务**：判断实例 $i$ 是否属于概念 $c$，即三元组 $(i, r_e, c)$ 是否成立（$r_e$ 表示 InstanceOf 关系）。
**核心目标**：

1. **外延空间**：实例 $i$ 的嵌入向量必须位于概念 $c$ 的椭球区域内（几何约束）。
2. **内涵空间**：实例 $i$ 的内涵语义应与概念 $c$ 的内涵语义相似（文本语义约束）。
通过结合两者，确保实例与概念在结构（所属关系）和语义（文本描述）上一致。

### 二、损失函数的数学表达式

InstanceOf 三元组的损失函数由 **外延空间损失** 和 **内涵空间损失** 两部分组成，公式为：

$$
f_{\text{ins}}(i, c) = f_{\text{ins}}^{\text{ex}}(i, c) + \alpha \cdot f_{\text{ins}}^{\text{in}}(i, c)
$$

其中：

- $f_{\text{ins}}^{\text{ex}}(i, c)$：外延空间几何约束损失
- $f_{\text{ins}}^{\text{in}}(i, c)$：内涵空间语义相似性损失
- $\alpha$：超参数，平衡两部分损失的权重


#### 三、外延空间损失 $f_{\text{ins}}^{\text{ex}}(i, c)$

##### 1. 公式定义

$$
f_{\text{ins}}^{\text{ex}}(i, c) = \left[ \sum_{j=1}^{d} \left( \frac{i_j^e - c_j}{b_j} \right)^2 - 1 \right]_+
$$

##### 2. 符号解析

- $i^e = (i_1^e, i_2^e, \ldots, i_d^e)$：实例 $i$ 在外延空间的嵌入向量（点坐标），维度为 $d$，通过实例嵌入矩阵 $E$ 初始化（可训练参数）。
- $c = (c_1, c_2, \ldots, c_d)$：概念 $c$ 在外延空间的椭球中心点（可训练参数），表示概念的“核心位置”。
- $b = (b_1, b_2, \ldots, b_d)$：概念 $c$ 在外延空间的椭球半轴长度向量（可训练参数），刻画概念在各维度上的扩展范围。
- $[x]_+$：ReLU 函数，即 $\max(0, x)$，仅当括号内值为正时激活，否则输出 0。


##### 3. 几何意义

- 椭球区域的数学定义为：$\sum_{j=1}^{d} \left( \frac{x_j - c_j}{b_j} \right)^2 \leq 1$，其中 $x_j$ 是空间点的坐标。
- 若实例 $i$ 属于概念 $c$，则 $i^e$ 必须满足上述不等式。损失函数计算实例点到椭球的“超出量”：
    - 当 $i^e$ 在椭球内时，$\sum \left( \frac{i_j^e - c_j}{b_j} \right)^2 \leq 1$，此时 $f_{\text{ins}}^{\text{ex}} = 0$（无损失）。
    - 当 $i^e$ 在椭球外时，超出部分被ReLU保留，作为损失值，迫使模型调整参数（$c, b, i^e$）将实例拉回概念区域内。


#### 四、内涵空间损失 $f_{\text{ins}}^{\text{in}}(i, c)$

##### 1. 公式定义

$$
f_{\text{ins}}^{\text{in}}(i, c) = 1 - \cos\left(i^i, c^i\right)
$$

或等价于欧式距离形式（根据原文，作者使用余弦相似度）：

$$
\cos\left(i^i, c^i\right) = \frac{i^i \cdot c^i}{\|i^i\| \|c^i\|}
$$

##### 2. 符号解析

- $i^i$：实例 $i$ 在内涵空间的虚拟嵌入向量，通过外延空间嵌入映射得到：$i^i = M_{ei} \cdot i^e$（$M_{ei}$ 是映射矩阵，实验中使用单位矩阵 $I$ 或可学习矩阵）。
- $c^i$：概念 $c$ 在内涵空间的嵌入向量，通过预训练语言模型（如 Sentence - BERT）对概念名称/描述编码得到（固定或微调参数）。
- $\cos(\cdot)$：余弦相似度，取值范围 $[-1, 1]$，值越大表示向量方向越接近。


##### 3. 设计原因

- 内涵空间聚焦概念的文本语义（如名称、描述），实例的内涵嵌入 $i^i$ 需与概念的内涵嵌入 $c^i$ 语义相似。
- 使用余弦相似度而非欧式距离，是因为余弦相似度更关注向量的“方向”相似性，适合捕捉语义相关性（如“猫”和“动物”的语义关联）。
- 损失函数设计为 $1 - \cos(\cdot)$，使得当语义相似时损失趋近于 0，不相似时损失趋近于 2（最大化间隔）。


#### 五、损失函数的设计逻辑与优势

##### 1. 为什么分两部分设计？

- **互补知识表示**：
    - 外延空间损失（几何约束）确保实例在结构上属于概念（硬约束，如“猫”必须属于“动物”的区域）。
    - 内涵空间损失（语义相似）确保实例与概念在文本语义上一致（软约束，如“猫”的描述应与“动物”的共性特征匹配）。
    - 两者结合避免单一空间的局限性：仅几何约束可能忽略概念的文本语义（如名称歧义），仅语义相似可能忽略层次结构（如实例必须严格属于某个概念）。


##### 2. 关键符号的作用总结

| 符号 | 含义 | 可训练性 | 作用 |
| :-- | :-- | :-- | :-- |
| $i^e, c, b$ | 外延空间实例点、概念中心点、半轴长度 | 是 | 定义实例与概念的几何隶属关系，通过梯度下降调整，使正确实例落入概念区域 |
| $i^i, c^i$ | 内涵空间实例（虚拟）、概念嵌入（预训练编码） | $i^i$ 依赖映射，$c^i$ 固定或微调 | 捕捉文本语义相似性，利用预训练模型的先验知识增强概念表示 |
| $\alpha$ | 损失权重超参数 | 否（人工设定） | 平衡几何约束与语义约束的重要性，通过网格搜索优化 |

##### 3. 训练中的优化目标

- 对于正样本 $(i, c)$：最小化 $f_{\text{ins}}(i, c)$，即迫使 $i^e$ 进入 $c$ 的椭球（$f_{\text{ins}}^{\text{ex}} \to 0$），且 $i^i$ 与 $c^i$ 语义相似（$f_{\text{ins}}^{\text{in}} \to 0$）。
- 对于负样本（随机替换实例或概念）：最大化损失，通过边际排名损失（如 $\gamma + f_{\text{ins}}(\text{正}) - f_{\text{ins}}(\text{负}) \geq 0$）拉开正负样本的差距。


#### 六、与传统方法的对比及创新点

- **传统几何方法（如 TransC、TransFG）**：仅在外延空间建模，用球体或椭球表示概念，但缺乏对概念文本语义的利用（如“哺乳动物”的“胎生”属性需通过文本描述体现）。
- **传统文本方法（如 Concept2Vec）**：仅通过名称或描述学习概念嵌入，忽略实例与概念的几何隶属关系（如无法保证实例必须属于概念区域）。
- **EIKE 的创新**：通过双空间联合损失，将结构知识（几何约束）与语义知识（文本编码）显性结合，使概念表示同时具备 **空间结构性** 和 **语义丰富性**，实验证明能显著提升分类和预测性能（如在 YAGO39K 数据集上 InstanceOf 分类准确率达 89.26%）。


### 总结

InstanceOf 三元组损失函数的设计核心是 **“结构约束 + 语义相似”的双重监督**：

1. **外延损失**通过椭球方程确保实例在几何上属于概念，利用可训练的中心点和半轴参数动态调整概念区域。
2. **内涵损失**通过预训练语言模型捕获概念的文本语义，迫使实例与概念在语义空间中保持相似。
3. 两者的加权和实现了对本体中外延知识（实例归属）和内涵知识（概念语义）的联合建模，是 EIKE 方法区别于传统模型的关键创新点之一。

### SubClassOf 三元组损失函数的详细解析

#### 一、SubClassOf 三元组的定义与核心目标

**任务**：判断概念 $c_i$ 是否为概念 $c_j$ 的子类，即三元组 $(c_i, r_c, c_j)$ 是否成立（$r_c$ 表示 SubClassOf 关系）。
**核心目标**：

1. **外延空间**：子类 $c_i$ 的所有实例必须属于父类 $c_j$，即 $c_i$ 的椭球区域必须完全包含于 $c_j$ 的椭球区域（几何包含约束）。
2. **内涵空间**：子类 $c_i$ 与父类 $c_j$ 应具有高度语义相似性，且父类作为更抽象的概念，其内涵表示的“信息量”应不低于子类（语义层次约束）。
通过结合两者，确保概念层次结构在几何空间和语义空间中一致。

### 二、损失函数的数学表达式

SubClassOf 三元组的损失函数同样由 **外延空间损失** 和 **内涵空间损失** 两部分组成，公式为：

$$
f_{\text{sub}}(c_i, c_j) = f_{\text{sub}}^{\text{ex}}(c_i, c_j) + \alpha \cdot f_{\text{sub}}^{\text{in}}(c_i, c_j)
$$

其中：

- $f_{\text{sub}}^{\text{ex}}(c_i, c_j)$：外延空间几何包含损失
- $f_{\text{sub}}^{\text{in}}(c_i, c_j)$：内涵空间语义层次损失
- $\alpha$：超参数，平衡两部分损失的权重


#### 三、外延空间损失 $f_{\text{sub}}^{\text{ex}}(c_i, c_j)$

##### 1. 公式定义

$$
f_{\text{sub}}^{\text{ex}}(c_i, c_j) = \left[ \sum_{j=1}^{d} \left( \frac{c_{i,j}}{b_{i,j}} - \frac{c_{j,j}}{b_{j,j}} \right)^2 + 1 - 1 \right]_+ \quad \text{（修正：原文公式应为）} \quad f_{\text{sub}}^{\text{ex}}(c_i, c_j) = \left[ \sum_{j=1}^{d} \left( \frac{c_{i,j} - c_{j,j}}{b_{j,j}} \right)^2 + \left( \frac{\|c_i - c_j\|}{b_j} \right)^2 - 1 \right]_+
$$

（注：根据原文第4.2节，正确形式应为确保子类椭球完全包含于父类椭球，几何包含的数学条件为：子类中心到父类中心的距离在各维度上的缩放后不超过父类半轴与子类半轴的差，具体推导如下）

**正确公式解析**（基于几何包含约束）：
若子类 $c_i$ 的椭球包含于父类 $c_j$ 的椭球，则对于任意点 $x \in G_{c_i}$，必有 $x \in G_{c_j}$。根据椭球方程，这要求：

$$
\sum_{k=1}^{d} \left( \frac{x_k - c_{i,k}}{b_{i,k}} \right)^2 \leq 1 \implies \sum_{k=1}^{d} \left( \frac{x_k - c_{j,k}}{b_{j,k}} \right)^2 \leq 1
$$

通过数学推导（简化版），损失函数设计为：

$$
f_{\text{sub}}^{\text{ex}}(c_i, c_j) = \left[ \sum_{k=1}^{d} \left( \frac{c_{i,k} - c_{j,k}}{b_{j,k}} \right)^2 + \left( \frac{b_{i,k}}{b_{j,k}} \right)^2 - 1 \right]_+
$$

（原文中可能简化为中心点和半轴的相对位置约束，具体以论文公式为准，此处以核心思想解析为主）

##### 2. 符号解析

- $c_i = (c_{i,1}, c_{i,2}, \ldots, c_{i,d})$：子类概念的椭球中心点（可训练参数）。
- $c_j = (c_{j,1}, c_{j,2}, \ldots, c_{j,d})$：父类概念的椭球中心点（可训练参数）。
- $b_i = (b_{i,1}, b_{i,2}, \ldots, b_{i,d})$：子类概念的半轴长度向量（可训练参数）。
- $b_j = (b_{j,1}, b_{j,2}, \ldots, b_{j,d})$：父类概念的半轴长度向量（可训练参数）。
- $[x]_+$：ReLU 函数，仅当子类椭球未完全包含于父类椭球时激活损失。


##### 3. 几何意义

- 子类椭球包含于父类椭球的条件：子类中心到父类中心的距离在各维度上的“缩放后偏移”，加上子类半轴与父类半轴的比例，不超过 1。
- 损失函数计算“包含误差”：若子类椭球部分超出父类区域，超出量被ReLU保留，迫使模型调整参数（$c_i, c_j, b_i, b_j$）使子类完全包含于父类。


#### 四、内涵空间损失 $f_{\text{sub}}^{\text{in}}(c_i, c_j)$

##### 1. 公式定义

$$
f_{\text{sub}}^{\text{in}}(c_i, c_j) = 1 - \cos\left(c_i^i, c_j^i\right) + \left( \|c_i^i\| - \|c_j^i\| \right)
$$

##### 2. 符号解析

- $c_i^i, c_j^i$：子类和父类在内涵空间的嵌入向量，通过预训练语言模型对概念名称/描述编码得到（固定或微调参数）。
- $\cos(\cdot)$：余弦相似度，衡量子类与父类的语义相似性（值越大越相似）。
- $\|c_i^i\|, \|c_j^i\|$：子类和父类内涵向量的模长，父类作为更抽象的概念，假设其模长应不小于子类（体现层次结构的“信息量”差异）。


##### 3. 设计原因

- **语义相似性**：子类与父类共享核心属性（如“猫”和“哺乳动物”都有“动物”的共性），余弦相似度确保两者在语义空间中接近。
- **模长约束**：父类概念更抽象，涵盖更多子类，其内涵表示的模长应更大（例如“动物”比“猫”包含更广泛的特征），通过 $\|c_i^i\| - \|c_j^i\| \leq 0$ 迫使父类模长不小于子类，保留层次结构的传递性（如 $c_i \subseteq c_j \subseteq c_k \implies \|c_i^i\| \leq \|c_j^i\| \leq \|c_k^i\|$）。


#### 五、损失函数的设计逻辑与优势

##### 1. 为什么分两部分设计？

- **几何包含的必要性**：
外延空间损失确保子类的所有实例必然属于父类（通过椭球包含关系），例如“猫”的实例点必须在“哺乳动物”的椭球内，传递性由几何约束自动保证（若 $c_i \subseteq c_j$ 且 $c_j \subseteq c_k$，则 $c_i \subseteq c_k$）。
- **语义层次的必要性**：
内涵空间损失确保概念在文本语义上符合层次结构，例如“哺乳动物”的描述应包含“猫”的共性特征，同时更抽象。模长约束避免语义颠倒（如父类不能比子类更具体）。


##### 2. 关键符号的作用总结

| 符号 | 含义 | 可训练性 | 作用 |
| :-- | :-- | :-- | :-- |
| $c_i, c_j$ | 子类/父类外延空间中心点 | 是 | 定义概念在几何空间中的核心位置，通过训练调整使子类中心靠近父类 |
| $b_i, b_j$ | 子类/父类外延空间半轴长度 | 是 | 控制概念在各维度的扩展范围，确保子类范围不超过父类 |
| $c_i^i, c_j^i$ | 子类/父类内涵空间嵌入向量 | 固定或微调 | 捕捉概念文本语义，余弦相似度确保语义相关，模长约束保留层次顺序 |
| $\alpha$ | 损失权重超参数 | 否（人工设定） | 平衡几何约束与语义约束的重要性，通过实验优化 |

##### 3. 训练中的优化目标

- 对于正样本 $(c_i, c_j)$：
    - 外延空间：最小化 $f_{\text{sub}}^{\text{ex}}$，迫使子类椭球完全包含于父类（$f_{\text{sub}}^{\text{ex}} \to 0$）。
    - 内涵空间：最大化余弦相似度（$\cos \to 1$），同时确保父类模长不小于子类（$\|c_j^i\| \geq \|c_i^i\|$，使 $\|c_i^i\| - \|c_j^i\| \leq 0$）。
- 对于负样本（随机替换子类或父类）：
    - 通过边际排名损失拉开正负样本的损失差距，确保正样本损失显著低于负样本。


#### 六、与传统方法的对比及创新点

- **传统几何方法（如 TransC）**：
仅通过球体或椭球的几何包含建模 SubClassOf 关系，但缺乏对概念语义的利用（例如“交通工具”和“汽车”的层次关系需结合文本描述中的“包含”语义）。
- **传统文本方法（如 Concept2Vec）**：
通过随机游走学习概念相似性，但无法显式建模层次结构的几何包含关系（例如无法保证子类实例必然属于父类区域）。
- **EIKE 的创新**：

1. **显式几何约束**：通过椭球包含关系强制保留层次结构的传递性，避免依赖隐含假设。
2. **语义层次监督**：结合预训练语言模型的语义编码，使概念嵌入同时捕获文本特征（如“哺乳动物”的“胎生”属性）和层次顺序（模长约束）。
实验表明，EIKE 在 SubClassOf 三元组分类上准确率比传统方法高 10%-20%（如在 YAGO39K 数据集上达 90.00%）。


### 总结

SubClassOf 三元组损失函数的设计核心是 **“几何包含 + 语义层次”的双重约束**：

1. **外延损失**通过椭球的数学包含条件，确保子类概念的所有实例在结构上属于父类，利用可训练的中心点和半轴参数动态调整概念区域的层次关系。
2. **内涵损失**通过语义相似度和模长约束，确保概念在文本语义上符合层次结构，父类更抽象且包含子类的共性特征。
3. 两者的结合使模型能够同时处理本体的结构层次（如类包含）和语义层次（如概念抽象度），是 EIKE 有效建模复杂本体关系的关键机制之一。
